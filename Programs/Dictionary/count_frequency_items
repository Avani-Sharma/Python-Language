# count frequency items 
items = ["apple", "banana", "apple", "orange", "banana", "apple"]
freq = {}
for i in items:
    if i in freq:
        freq[i] += 1
    else:
        freq[i] = 1
print(freq)

# count all occurence of all elements from list 
li = [1,9,2,3,4,5,6,7,5,3,2,2]
freq = {}
for i in li:
    if i in freq:
        freq[i] += 1
    else:
        freq[i] = 1
print(freq)
